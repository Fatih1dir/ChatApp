import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    searchInput: {
      height: 40,
      borderColor: 'gray',
      borderWidth: 1,
      margin: 10,
      paddingLeft: 10,
      borderRadius:10,
      fontWeight:'bold'
    },
  });

export default styles;